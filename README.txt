Tsz Kit Lo (912404574)
ECS 153
Lab 4

How to compile it?

Compile the source file by "make" command. it will run the Makefile.

What it does?

runpriv will call sniff and change the sniff's protection mode and ownership.

NOTE: If chown command fail, runpriv will return the system(3) return value.
